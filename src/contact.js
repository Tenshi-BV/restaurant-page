const contact = () => {

    const pContact = document.createElement('div');
    pContact.textContent = 'Email us at bigbrain@yourhead.eu';
    pContact.setAttribute('id', 'pcontact');
    buttonContent.appendChild(pContact);

};
    
export { contact }